<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wzmtr.dom.mapper.vehicle.CrewBusinessMapper">

    <select id="page" resultType="com.wzmtr.dom.dto.res.vehicle.CrewBusinessResDTO">
        SELECT ID, BUSINESS_DESC, VERSION, BELONG_DATE, DATA_TYPE, START_DATE, END_DATE
        FROM VEHICLE_CREW_BUSINESS
        WHERE DEL_FLAG='0'
        <if test="dataType=='1'">
            <if test="startDate!=null and startDate!=''">
                AND BELONG_DATE>=to_date(#{startDate},'YYYY-MM-DD')
            </if>
            <if test="endDate!=null and endDate!=''">
                AND to_date(#{endDate},'YYYY-MM-DD')>=BELONG_DATE
            </if>
            ORDER BY BELONG_DATE desc
        </if>
        <if test="dataType!='1'">
            <if test="startDate!=null and startDate!=''">
                AND START_DATE>=to_date(#{startDate},'YYYY-MM-DD')
            </if>
            <if test="endDate!=null and endDate!=''">
                AND to_date(#{endDate},'YYYY-MM-DD')>=END_DATE
            </if>
            ORDER BY START_DATE desc
        </if>
    </select>

    <select id="detail" resultType="com.wzmtr.dom.dto.res.vehicle.CrewBusinessResDTO">
        SELECT ID, BUSINESS_DESC, VERSION, BELONG_DATE, DATA_TYPE, START_DATE, END_DATE
        FROM VEHICLE_CREW_BUSINESS
        WHERE DEL_FLAG='0' AND ID=#{id}
    </select>

    <select id="selectIsExist" resultType="java.lang.Integer">
        SELECT count(1) FROM VEHICLE_CREW_BUSINESS
        WHERE
        <if test="dataType=='1'">
            BELONG_DATE=#{belongDate,jdbcType=TIMESTAMP}
        </if>
        <if test="dataType!='1'">
            START_DATE=#{startDate,jdbcType=TIMESTAMP} AND END_DATE=#{endDate,jdbcType=TIMESTAMP}
        </if>
        <if test="version!=null and version!=''">
            AND VERSION=#{version,jdbcType=VARCHAR}
        </if>
        AND DEL_FLAG='0' AND rownum=1
    </select>

    <insert id="add">
        INSERT INTO VEHICLE_CREW_BUSINESS
        (ID,
        <if test="businessDesc!=null and businessDesc!=''">
            BUSINESS_DESC,
        </if>
        <if test="dataType!=null and dataType!=''">
            DATA_TYPE,
        </if>
        <if test="startDate!=null and startDate!=''">
            START_DATE,
        </if>
        <if test="endDate!=null and endDate!=''">
            END_DATE,
        </if>
        CREATE_BY, CREATE_DATE, BELONG_DATE)
        VALUES (#{id},
        <if test="businessDesc!=null and businessDesc!=''">
            #{businessDesc},
        </if>
        <if test="dataType!=null and dataType!=''">
            #{dataType},
        </if>
        <if test="startDate!=null and startDate!=''">
            #{startDate},
        </if>
        <if test="endDate!=null and endDate!=''">
            #{endDate},
        </if>
        #{createBy}, sysdate, #{belongDate})
    </insert>

    <update id="modify">
        UPDATE VEHICLE_CREW_BUSINESS SET
        <if test="businessDesc!=null">
            BUSINESS_DESC=#{businessDesc},
        </if>
        <if test="dataType!=null">
            DATA_TYPE=#{dataType},
        </if>
        <if test="startDate!=null">
            START_DATE=#{startDate},
        </if>
        <if test="endDate!=null">
            END_DATE=#{endDate},
        </if>
        VERSION=VERSION+1, UPDATE_BY=#{updateBy}, UPDATE_DATE=sysdate
        WHERE ID=#{id} and VERSION=#{version}
    </update>

    <update id="delete">
        UPDATE VEHICLE_CREW_BUSINESS SET
        DEL_FLAG='1', UPDATE_BY=#{userId}, UPDATE_DATE=sysdate
        WHERE ID IN (
        <foreach collection="ids" index="index" item="id" separator=",">
            #{id}
        </foreach>
        )
    </update>

</mapper>